# Server Configuration
# Port 3000 is used internally (Docker exposes as 4141 externally)
PORT=3000
HOST=localhost

# Database Path
# Default for local development
DB_PATH=./sqlite.db
# For Docker: DB_PATH=/app/db/sqlite.db (set in docker-compose.yml)

# Google OAuth (for Google Drive integration)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# For Docker deployment, use your server's domain/IP with port 4141
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback/google

# Better Auth
BETTER_AUTH_SECRET=
# For Docker deployment, use your server's domain/IP with port 4141
BETTER_AUTH_URL=http://localhost:3000
